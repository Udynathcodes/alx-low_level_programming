#include "main.h"
/**
 * _abs -  computes the absolute value of an integer
 * @n: parameter
 * Return: 0 and 1 (negative or positive)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
		return (n);
}
