#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @y: The first integer
 * @z: The second integer
 * Return: Always 0
 */

int mul(int y, int z)
{
	int result = y * z;

	return (result);
}

